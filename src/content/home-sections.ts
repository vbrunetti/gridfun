/** Cover-flow sections after hero chapters — each gets a scroll slide and rail dot. */
export type HomeCoverSection = {
  id: string;
  label: string;
};

export const homeCoverSections: HomeCoverSection[] = [
  { id: "home-secondary", label: "Home secondary" },
];
